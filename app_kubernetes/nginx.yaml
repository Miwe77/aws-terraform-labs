apiVersion: apps/v1
kind: Deployment
metadata:
  name: servidor-nginx
spec:
  replicas: 1
  selector:
    matchLabels:
      app: servidor-nginx
  template:
    metadata:
      labels:
        app: servidor-nginx
    spec:
      containers:
      - name: nginx
        image: nginx:alpine
        ports:
        - containerPort: 80
---
apiVersion: v1
kind: Service
metadata:
  name: servidor-nginx-svc
spec:
  selector:
    app: servidor-nginx
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80