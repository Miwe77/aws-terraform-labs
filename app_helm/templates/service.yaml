apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-servicio
spec:
  selector:
    app: {{ .Release.Name }}
  ports:
    - protocol: TCP
      port: {{ .Values.service.puertoExterno }}
      targetPort: 5678